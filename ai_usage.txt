AI USAGE LOG Practice 3: a parser and an abstract syntax tree
Languages and Compilers Design

Інструмент: Claude Code (Anthropic), модель Claude Sonnet 5.

Як використовувався AI:
- Отримав завдання у вигляді PDF-файлу практики (w03-practice.pdf) поверх
  наявних lexer.py і compiler.py з Practice 2.
- Написав grammar.ebnf — граматику мови (Task 1) з правилами program,
  statement, decl, assign, exit_stmt та трирівневою expr/term/factor для
  Task 4 (пріоритет * над +/-, ліва асоціативність), звірену з токенами,
  які видає lexer.py.
- Спроєктував ast_nodes.py — ієрархію класів дерева (ProgramNode,
  DeclNode, AssignNode, ExitNode, BinOpNode, VarNode, ConstNode) з
  методом dump() для --ast.
- Написав parser.py — ручний рекурсивний спускний парсер (Task 2 і
  Task 4): peek()/eat(), по одному методу на правило граматики, одне
  випередження токеном, без парсер-генераторів, регулярних виразів чи
  eval. Помилки повідомляються з line:column токена, що порушив правило.
- Написав gen.py — генерацію коду як обхід дерева (Task 3): клас CodeGen
  (visitor) з одним методом на вид вузла, той самий llvmlite.ir builder
  API, що й раніше; таблиця символів і перевірки "оголошено один раз",
  "оголошено перед використанням", "mut перед :=" залишились у цьому
  обході. Єдиний текст IR — str(module).
- Переписав compiler.py: lexer -> parser -> CodeGen, додав прапорець
  --ast (друкує дерево, нічого не записує), зберіг --tokens.
- Додав 5 нових коректних і 5 нових некоректних тестів у tests/ для
  Task 4 (2+3*4, ланцюжок з * посередині, лівоасоціативний ланцюжок -,
  вираз праворуч від :=, повний приклад із методички на 120; два
  оператори підряд, оператор в кінці рядка, оператор одразу після {,
  токени після завершеної інструкції, використання змінної до
  оголошення всередині виразу), кожен з очікуваним stdout/stderr, і
  декілька .ast файлів з очікуваним дампом дерева.
- Після ручного зауваження про формат виводу (--tokens не показував
  line:column, а позиція помилки "немає {}" залежала від того, який
  токен випадково останній у рядку) виправив Token.__repr__ у lexer.py
  та зробив позицію цієї помилки в parser.py явно "одразу після імені".
- Оновив README.md під нову структуру файлів.

Перевірка результатів (виконано вручну студентом):
- --ast звірено з обома прикладами з PDF: робочий приклад Practice 2
  (Program exit with result 70) та i32 x{2 + 3 * 4} — обидва дерева
  збігаються з рисунками в методичці.
- Повний приклад Task 4 (x, a, z) прогнано через lli та через
  llc + clang до бінарника — обидва дають "Program exit with result 120".
- Усі 22 тести в tests/ (11 коректних, 11 некоректних) пройдено через
  tests/run_tests.py (22/22 passed), включно з усіма 12 тестами
  Practice 2 — регресій нема.
- Повідомлення про помилки (line:column) перевірено вручну для кожного
  випадку зі списку в PDF: два оператори підряд, оператор в кінці рядка,
  оператор після {, токени після інструкції, змінна до оголошення,
  інструкція після exit, програма без exit.

Що зроблено самостійно (без AI):
- Створення гілки practice-3 для цієї практики (без пушу на GitHub —
  буде запушено пізніше).
- Фінальний перегляд коду та ручне тестування граничних випадків перед
  здачею викладачу.
